{
  "$schema": "../schemas/components.schema.json",
  "componentId": "citation",
  "name": "Source Citation",
  "description": "External source reference for GEO optimization (Princeton method: cite-sources)",
  "category": "geo",
  "markdownTemplate": "According to [{source}]({url}), {statement}",
  "properties": {
    "statement": {
      "type": "string",
      "required": true,
      "description": "The statement or claim being cited",
      "minLength": 30,
      "maxLength": 300
    },
    "source": {
      "type": "string",
      "required": true,
      "description": "Name of the source (e.g., 'OpenTelemetry Docs', 'W3C Standard')"
    },
    "url": {
      "type": "string",
      "required": true,
      "description": "URL to the source"
    },
    "sourceType": {
      "type": "string",
      "required": false,
      "enum": ["official-docs", "research-paper", "industry-report", "blog-post", "standard", "book"],
      "description": "Type of source"
    },
    "date": {
      "type": "string",
      "required": false,
      "description": "Publication or update date (YYYY-MM-DD)"
    }
  },
  "validation": {
    "requireSource": true,
    "requireUrl": true,
    "minLength": 30
  },
  "geoOptimized": true,
  "geoMethod": "cite-sources",
  "princetonStudy": {
    "effectiveness": "high",
    "visibilityIncrease": "115% for lower-ranked sites",
    "bestFor": [
      "All content types",
      "Technical documentation",
      "Educational content",
      "Research-based articles"
    ],
    "placement": "Throughout content, especially for key claims"
  },
  "guidelines": [
    "Cite authoritative sources (official docs, research, standards)",
    "Link directly to the relevant page, not just homepage",
    "Prefer recent sources (within 1-2 years)",
    "Use citations to support key claims and technical details",
    "Cite 5-7 sources minimum for comprehensive articles"
  ],
  "examples": [
    {
      "statement": "OpenTelemetry provides vendor-agnostic instrumentation libraries for distributed tracing, metrics, and logs",
      "source": "OpenTelemetry Documentation",
      "url": "https://opentelemetry.io/docs/what-is-opentelemetry/",
      "sourceType": "official-docs",
      "date": "2024-01-15"
    },
    {
      "statement": "the HTTP/2 protocol reduces latency through header compression and multiplexing",
      "source": "RFC 7540 - HTTP/2 Specification",
      "url": "https://www.rfc-editor.org/rfc/rfc7540",
      "sourceType": "standard"
    }
  ]
}
